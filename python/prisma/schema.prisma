datasource db {
    provider = "sqlite"
    url      = "file:/Users/mattfirth/Library/Application Support/truffle-app/truffle.test.db"
}

generator db {
    provider             = "prisma-client-py"
    interface            = "asyncio"
    recursive_type_depth = 5
}

model RunningModels {
    id           String @id
    instance     Int
    name         String
    size         BigInt
    pid          Int
    port         Int
    quantization String
}
